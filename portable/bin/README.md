# llama.cpp runtime

Place a Windows llama.cpp runtime here. LanternLM expects
`llama-server.exe` and the DLL files shipped with the same build.

Do not commit runtime binaries. Keep the upstream license and release
information with any binary distribution.
